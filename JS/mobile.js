// const ajuste = document.querySelectorAll('.ajuste');
// console.log(ajuste)

// // Verifica se o media query está sendo aplicado
// const isSmallScreen = window.matchMedia('(max-width: 414px)').matches;

// // Se o media query estiver sendo aplicado, altera a cor do fundo para azul
// if (isSmallScreen) {
//     ajuste.forEach(elemento => {
//         elemento.classList.remove('col-md-auto')
//         elemento.classList.add('col')
//         elemento.classList.add('mt-2')
//       });
//   a
// }